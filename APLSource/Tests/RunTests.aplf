 RunTests←{
     _←##.Init''
     r←1↓refs ⎕THIS
     ⎕THIS._defaultBucket←'awstools-test'
     ⍝ Make sure to set correct geogrpahic endpoint.
     ⍝ Getting details on a bucket created in us-east-2 will fail
     ⍝ when client is set to us-east-1
     client←##.Client.New's3' 'us-east-1'
     _defaultBucketRef←getBucket client

     out←client{
         list←⍵.⎕NL ¯3
         err←⍵{
             exp←'⍺.',⍵,' client'
             ⍎exp
         }¨list
         ⎕←err/list
         ⎕←(⊂(⍕⍵),'.'),¨(err/list)
         err
⍝     }¨r
     }S3
     1:_←out
 }
